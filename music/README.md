# Music Directory

Place your `.mp3`, `.flac`, `.wav`, `.ogg`, `.m4a`, or `.aac` audio files in this directory (or subfolders).

The `playerd` daemon automatically scans this directory recursively for supported music files and updates the player library.
